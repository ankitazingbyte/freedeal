<%= form_for @deal do |form| %>
  <% if @deal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deal.errors.count, "error") %> prohibited this deal from being saved:</h2>
      <ul>
        <% deal.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :deal_image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :deal_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :deal_description %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url_link %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
