<%= form_with(model: latest, local: true) do |form| %>
  <% if latest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(latest.errors.count, "error") %> prohibited this latest from being saved:</h2>

      <ul>
      <% latest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :latest_image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :latest_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :latest_description %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :latest_link %>
  </div>

  <div class="field">
    <%= form.label :discount %>
    <%= form.text_field :discount, id: :latest_discount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
